#cloud-config
# vim: syntax=yaml
# -------------------------------------------------------------------------------
# Mini networking node: Ubuntu + BIND9 VAST forwarder + minimized net tools.
# Default ubuntu user. Shell: basic aliases + set -o vi.
#
# Day-2 knobs (Terraform): vast_zones + vast_dns_ips
#
# Author: Karl Vietmeier
# -------------------------------------------------------------------------------

package_update: true
package_upgrade: false

packages:
  # BIND
  - bind9
  - bind9utils
  - dnsutils
  # Network troubleshooting
  - iproute2
  - iputils-ping
  - net-tools
  - traceroute
  - mtr-tiny
  - tcpdump
  - nmap
  - curl
  - wget
  # Light QoL
  - vim
  - jq
  - python3
  - less
  - htop
  - tmux

write_files:
  - path: /etc/systemd/resolved.conf.d/bind-forwarder.conf
    permissions: "0644"
    content: |
      [Resolve]
      DNS=${cloud_resolver}
      DNSStubListener=no

  - path: /etc/bind/named.conf.options
    permissions: "0644"
    content: |
      // Managed by cloud-init — BIND VAST conditional forwarder
      acl internals {
      %{ for cidr in query_cidrs ~}
          ${cidr};
      %{ endfor ~}
      };

      options {
          directory "/var/cache/bind";
          allow-query { internals; localhost; };
          allow-recursion { internals; localhost; };
          forwarders {
              ${cloud_resolver};
          };
          forward only;
          dnssec-validation auto;
          listen-on port 53 { any; };
          listen-on-v6 { any; };
          recursion yes;
      };

  - path: /etc/bind/named.conf.local
    permissions: "0644"
    content: |
      // Managed by cloud-init — VAST zone forwarders
      %{ for zone in vast_zones ~}
      zone "${zone}" {
          type forward;
          forward only;
          forwarders {
      %{ for ip in vast_dns_ips ~}
              ${ip};
      %{ endfor ~}
          };
      };
      %{ endfor ~}

  - path: /etc/skel/.bashrc.bind_fwd
    permissions: "0644"
    content: |
      # >>> bind_fwd aliases >>>
      alias la="ls -Av"
      alias ls="ls -hF --color=auto"
      alias l="ls -CFv"
      alias ll='ls -lhvF --group-directories-first'
      alias lla='ls -alhvF --group-directories-first'
      alias grep='grep --color=auto'
      alias cdb='cd -'
      alias cdu='cd ..'
      set -o vi
      bind 'set bell-style none' 2>/dev/null || true
      # <<< bind_fwd aliases <<<

runcmd:
  - |
    for rc in /home/ubuntu/.bashrc /root/.bashrc; do
      touch "$rc"
      grep -qF 'bind_fwd aliases' "$rc" 2>/dev/null || cat /etc/skel/.bashrc.bind_fwd >> "$rc"
    done
    chown ubuntu:ubuntu /home/ubuntu/.bashrc 2>/dev/null || true
  - [bash, -c, "ln -sf /run/systemd/resolve/resolv.conf /etc/resolv.conf || true"]
  - [systemctl, restart, systemd-resolved]
  - [named-checkconf, /etc/bind/named.conf]
  - [bash, -c, "systemctl enable --now bind9.service 2>/dev/null || systemctl enable --now named.service"]
  - [bash, -c, "systemctl restart bind9.service 2>/dev/null || systemctl restart named.service"]

final_message: "========= BIND mini networking node ready ========="
